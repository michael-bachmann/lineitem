// FeedbackForm now lives in @lineitem/ui (shared with the landing page).
// Re-exported here so existing imports (`@/components/FeedbackForm`) keep working.
export { FeedbackForm as default } from "@lineitem/ui";
