# Web3Forms access key for the Help & About feedback forms.
# Publishable (it only routes mail to the owner's inbox). Optional — the code
# falls back to the project's public key if this is unset.
VITE_WEB3FORMS_KEY=your-web3forms-access-key
